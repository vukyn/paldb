# MkDocs build output
site/

# Python venv
.venv/
venv/
__pycache__/
*.pyc

# Editor / OS
.DS_Store
.vscode/
.idea/

# Code knowledge graph (platform local artifact — never commit)
.code-review-graph/

# Agent / tooling internal state — never commit into the repo …
.claude/*
# … except the shareable slash-commands (portable across machines with the repo)
!.claude/commands/
